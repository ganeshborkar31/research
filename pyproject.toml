[project]
name = "knowledge-ai"
version = "0.1.0"
description = "Enterprise RAG + research ingestion platform"
requires-python = ">=3.10"

dependencies = [
  # Core API
  "fastapi",
  "httpx",
  "uvicorn",
  "debugpy",
  "python-multipart",     # form parsing (twilio webhook)
  # AI / RAG
  "langchain",
  "langchain-openai",
  "google-genai",         # gemini sdk
  "qdrant-client",
  # Config
  "python-dotenv",
  "pydantic-settings",
  "PyJWT",
  # Observability
  "prometheus-client",
  "prometheus-fastapi-instrumentator",
  # Database
  "asyncpg",              # async driver (app)
  "psycopg[binary]",      # sync driver (alembic)
  "alembic",
  # Messaging / Cache
  "aio-pika",
  "redis",
  "twilio",               # voice call integrations
  "pydantic[email]>=2.12.5",
]

# ---------------------------------------
# Test Dependencies (NOT installed in prod)
# ---------------------------------------
[project.optional-dependencies]
test = [
  "pytest",
  "pytest-asyncio",
  "testcontainers[postgresql,redis]",

]
