global:
  scrape_interval: 15s

scrape_configs:
  - job_name: "fastapi"
    static_configs:
      - targets: ["api:8000"]

  - job_name: "workers"
    static_configs:
      - targets: ["api:8000"]  # workers expose metrics too
